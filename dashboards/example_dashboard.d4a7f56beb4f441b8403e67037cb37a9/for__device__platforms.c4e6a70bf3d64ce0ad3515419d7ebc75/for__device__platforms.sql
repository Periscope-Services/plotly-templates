-- ▲ Click the title to see how filter names name can be dynamically substituted.

-- Use number overlays to display text on a dashboard - https://doc.periscopedata.com/article/graph-like-chart-types#content

-- Number Overlay charts can also be used to display string values returned from a SQL statement. The selected text from the query acts as the larger font, while the chart title acts as the smaller font for the label.

select 'Top Level KPI'